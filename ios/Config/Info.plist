<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- 나머지 키는 빌드 설정(INFOPLIST_KEY_*)이 만든다. 여기는 사전형이라
	     설정으로 못 적는 것만 — 컨테이너를 파일 앱·Finder 에 「LazyMemo」 폴더로
	     보이게 하는 선언. 이것이 없으면 동기화는 되어도 사용자 눈에는 안 보인다
	     (D4 의 「Finder 로 열어도 된다」가 깨진다). -->
	<key>NSUbiquitousContainers</key>
	<dict>
		<key>iCloud.io.github.bunhine0452.lazymemo</key>
		<dict>
			<key>NSUbiquitousContainerIsDocumentScopePublic</key>
			<true/>
			<key>NSUbiquitousContainerName</key>
			<string>LazyMemo</string>
			<key>NSUbiquitousContainerSupportedFolderLevels</key>
			<string>Any</string>
		</dict>
	</dict>
</dict>
</plist>
